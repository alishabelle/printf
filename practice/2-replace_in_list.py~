#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    newList = my_list.copy()
    if idx < 0 or idx >= len(my_list):
        return my_list
    for idx in newList:
        newList[idx] = element
